package com.latam.alura;

import javax.swing.*;

class ValorConversion {
    public static void mostrarResultado(double result) {
        JOptionPane.showMessageDialog(null, "Resultado de la conversión: " + result);
    }
}